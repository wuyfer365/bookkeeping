<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookkeeping.mapper.CategoryMapper">

    <resultMap id="CategoryMap" type="com.bookkeeping.entity.Category">
        <id     property="id"        column="id"/>
        <result property="name"      column="name"/>
        <result property="parentId"  column="parent_id"/>
        <result property="level"     column="level"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="CategoryMap">
        SELECT id, name, parent_id, level, sort_order, created_at, updated_at
        FROM categories
        ORDER BY level ASC, sort_order ASC
    </select>

    <select id="findById" resultMap="CategoryMap">
        SELECT id, name, parent_id, level, sort_order, created_at, updated_at
        FROM categories WHERE id = #{id}
    </select>

    <select id="findByParentId" resultMap="CategoryMap">
        SELECT id, name, parent_id, level, sort_order, created_at, updated_at
        FROM categories WHERE parent_id = #{parentId}
        ORDER BY sort_order ASC
    </select>

    <insert id="insert">
        <selectKey keyProperty="id" resultType="int" order="AFTER">
            SELECT last_insert_rowid()
        </selectKey>
        INSERT INTO categories (name, parent_id, level, sort_order)
        VALUES (#{name}, #{parentId}, #{level}, #{sortOrder})
    </insert>

    <update id="update">
        UPDATE categories
        SET name = #{name}, sort_order = #{sortOrder}, updated_at = datetime('now','localtime')
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM categories WHERE id = #{id}
    </delete>

    <select id="countByParentId" resultType="int">
        SELECT COUNT(*) FROM categories WHERE parent_id = #{parentId}
    </select>

    <select id="countExpensesByCategoryId" resultType="int">
        SELECT COUNT(*) FROM expenses WHERE category_id = #{categoryId}
    </select>

</mapper>
